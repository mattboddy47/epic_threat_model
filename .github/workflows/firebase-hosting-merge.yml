# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    # strategy:
    #   matrix:
    #     node-version: [15.x]

    steps:
      - uses: actions/checkout@v2
      # - name: Use Node.js version ${{ matrix.node-version }}
      #   uses: actions/setup-node@v2
      #   with: 
      #     node-version: ${{ matrix.node-version }}
      - run: npm i ngx-text-diff --legacy-peer-deps
      - run: npm install @mui/material axios firebase-tools react react-native victory victory-chart @pmmmwh/react-refresh-webpack-plugin react-refresh --legacy-peer-deps
      # - run: yarn add @mui/material
      # - run: yarn add axios
      # - run: yarn add firebase-tools
      # - run: yarn add react
      # - run: yarn add react-native
      # - run: yarn add victory
      # - run: yarn add victory-chart
      - run: npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_SIMPLE_THREAT_MODEL }}'
          channelId: live
          projectId: simple-threat-model
